# requirements-nvidia.txt (For NVIDIA GPU Installation with CUDA 12.1)
# This file ensures a compatible set of CUDA-enabled torch packages are installed.

# PyTorch is installed separately in Dockerfile for better control
# --extra-index-url https://download.pytorch.org/whl/cu121
# torch==2.5.1
# torchvision==0.20.1  
# torchaudio==2.5.1

# --- Core Web Framework (install first) ---
fastapi
uvicorn[standard]

# Other ML & Audio Libraries (flexible versions for compatibility)
numpy>=1.24.0,<2.0.0  # More flexible range
soundfile  # Requires libsndfile system library
librosa  # Let it choose compatible version
safetensors  # Let it choose compatible version
descript-audio-codec
transformers>=4.40.0  # Minimum required version
diffusers>=0.26.0  # Minimum required version
s3tokenizer  # Required for original chatterbox
resemble-perth  # Let it choose compatible version
conformer  # Let it choose compatible version
pkuseg  # Let it choose compatible version
pykakasi  # Let it choose compatible version

# Configuration & Utilities
PyYAML
python-multipart
requests
Jinja2
watchdog
aiofiles
unidecode
inflect
tqdm
hf_transfer                     # Speed up file transfers

# Audio Post-processing
pydub
audiotsm
praat-parselmouth

# --- Install chatterbox LAST (after all dependencies) ---
# Install chatterbox from ORIGINAL ResembleAI repository with multilingual support
git+https://github.com/resemble-ai/chatterbox.git
