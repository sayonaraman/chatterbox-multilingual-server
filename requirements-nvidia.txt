# requirements-nvidia.txt (For NVIDIA GPU Installation with CUDA 12.1)
# This file ensures a compatible set of CUDA-enabled torch packages are installed.

# PyTorch is installed separately in Dockerfile for better control
# --extra-index-url https://download.pytorch.org/whl/cu121
# torch==2.5.1
# torchvision==0.20.1  
# torchaudio==2.5.1

# --- Core Web Framework (install first) ---
fastapi
uvicorn[standard]

# Other ML & Audio Libraries (EXACT versions from original chatterbox pyproject.toml)
numpy>=1.24.0,<1.26.0  # Exact range from original
soundfile  # Requires libsndfile system library
librosa==0.11.0  # Exact version from original
safetensors==0.5.3  # Exact version from original
descript-audio-codec
transformers==4.46.3  # Exact version from original
diffusers==0.29.0  # Exact version from original
s3tokenizer  # Required for original chatterbox
resemble-perth==1.0.1  # Exact version from original
conformer==0.3.2  # Exact version from original
pkuseg==0.0.25  # Exact version from original
pykakasi==2.3.0  # Exact version from original

# Configuration & Utilities
PyYAML
python-multipart
requests
Jinja2
watchdog
aiofiles
unidecode
inflect
tqdm
hf_transfer                     # Speed up file transfers

# Audio Post-processing
pydub
audiotsm
praat-parselmouth

# --- Install chatterbox LAST (after all dependencies) ---
# Install chatterbox from ORIGINAL ResembleAI repository with multilingual support
git+https://github.com/resemble-ai/chatterbox.git
