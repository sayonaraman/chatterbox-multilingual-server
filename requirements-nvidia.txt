# requirements-nvidia.txt (For NVIDIA GPU Installation with CUDA 12.1)
# This file ensures a compatible set of CUDA-enabled torch packages are installed.

# Use the official PyTorch package index for CUDA 12.8 (matching Dockerfile)
--extra-index-url https://download.pytorch.org/whl/cu128

# Pinning a compatible set of torch-related libraries to match original chatterbox
# Updated to use PyTorch 2.6.0 for compatibility with original ResembleAI chatterbox
torch==2.6.0
torchvision==0.21.0  # Compatible with torch 2.6.0
torchaudio==2.6.0

# --- Core Application Dependencies ---

# Install chatterbox from ORIGINAL ResembleAI repository with multilingual support
git+https://github.com/resemble-ai/chatterbox.git

# Core Web Framework
fastapi
uvicorn[standard]

# Other ML & Audio Libraries (matching original chatterbox requirements)
numpy>=1.24.0,<1.26.0  # Exact version from original chatterbox
soundfile  # Requires libsndfile system library
librosa==0.11.0  # Exact version from original chatterbox
safetensors==0.5.3  # Exact version from original chatterbox
descript-audio-codec
transformers==4.46.3  # Required for original chatterbox
diffusers==0.29.0  # Required for original chatterbox
s3tokenizer  # Required for original chatterbox
resemble-perth==1.0.1  # Required for original chatterbox
conformer==0.3.2  # Required for original chatterbox
pkuseg==0.0.25  # Required for original chatterbox
pykakasi==2.3.0  # Required for original chatterbox

# Configuration & Utilities
PyYAML
python-multipart
requests
Jinja2
watchdog
aiofiles
unidecode
inflect
tqdm
hf_transfer                     # Speed up file transfers

# Audio Post-processing
pydub
audiotsm
praat-parselmouth
